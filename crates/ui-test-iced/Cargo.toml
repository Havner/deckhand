[package]
name = "ui-test-iced"
edition.workspace = true
license.workspace = true
publish = false
version.workspace = true

[[bin]]
name = "deckhand-ui-iced"
path = "src/main.rs"

[dependencies]
ui-test-common = { path = "../ui-test-common" }
ipc = { path = "../ipc" }
config = { path = "../config" }
# Pinned to released 0.14 (the workspace `iced` tracks git 0.15-dev for the real `deckhand` UI); this
# bake-off prototype stays put.
iced = "0.14"
rfd.workspace = true
